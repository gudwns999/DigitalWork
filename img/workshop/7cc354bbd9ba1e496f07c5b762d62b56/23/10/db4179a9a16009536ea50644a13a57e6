(lab9-2: Shortest Path Algorithm - Step2) 
첨부한 lab9_1_sol.html을 수정하여 다음 기능을 
구현한 후, 작성된 파일은 lab9_2_sol.html로 이름을 변경하고 
lab9_2_sol.zip로 압축하여 가상대학에 제출하라. 
-------------------------------------------------------

(요구기능) lab9_1_sol.html의 <최소시간노드>, <노드찾기>,  
<노드업데이트> 버튼 기능을 완성한다.
 
1) 즉, <최소시간노드> 버튼을 누르면 <in-queue> 텍스트창에 있는 줄 중에서 세번째 값(경과시간)이 제일 작은 줄을 삭제한 
나머지를 <out-queue> 텍스트창에 출력한다. 이 작업은 코드 구현 
관점에서 보면 minTest()에서 호출하는 함수 minDrop()을 구현하는 것에 해당된다.

2) 또한, <노드찾기> 버튼을 누르면 <in-queue> 텍스트창에 있는 
줄의 첫번째 값들 중에서 <노드이름> 텍스트창에 있는 값과  
일치하는 줄을 찾아 해당 줄의 인덱스값과 함께 <찾기결과> 
텍스트창에 출력한다. 이 작업은 코드 구현 관점에서 보면 
findTest()에서 호출하는 함수 findIndex()을 구현하는 것에 
해당된다.

3) 마지막으로 <노드업데이트> 버튼을 누르면 <노드업데이트> 
텍스트창에 있는 줄의 첫번째 값과 일치하는 줄을 <in-queue> 
텍스트창에서 찾되 없는 경우, <in-queue> 텍스트창의 문자열을 
복사한 후 이어서 <노드업데이트> 텍스트창에 있는 줄을 
append한 결과를 <out-queue> 텍스트창에 출력한다.
그외의 경우, <in-queue> 텍스트창에 있는 해당 줄(전자)의 
세번째값이 <노드업데이트> 텍스트창의 세번째 값보다 크면 
전자의 위치에 <노드업데이트> 텍스트창의 복사본의 해당줄을
<in-queue> 텍스트창값으로 교체한 후 <out-queue> 텍스트창에 
출력한다. 작은 경우는 아무 수정없이 <out-queue> 텍스트창에 
출력한다. 이 작업은 코드 구현 관점에서 보면 updateTest()에서 
호출하는 함수 updateQueue()을 구현하는 것에 해당된다.
